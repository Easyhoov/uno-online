# UNO Online 功能更新 v1.2.0

## 🎉 新增功能

### 1. 房主掉线处理 - 房主转移 ✅

**功能说明：**
- 房主掉线时，自动将房主权限转移给第一个加入的客户端玩家
- 所有玩家会收到房主转移通知
- 新房主继承游戏管理权限（踢人、开始游戏等）

**实现细节：**
- 新增 `HOST_TRANSFER` 消息类型
- 房主断开时调用 `transferHost()` 方法
- 自动选择第一个客户端作为新房主

**使用场景：**
- 房主网络不稳定掉线
- 房主主动离开房间

---

### 2. 断线重连 - 保留游戏状态 ✅

**功能说明：**
- 玩家掉线后 30 秒内可以重连回原游戏
- 重连后恢复完整游戏状态（手牌、游戏进度、当前回合等）
- 超时后玩家被移除，游戏继续

**实现细节：**
- 新增 `RECONNECT_REQUEST` / `RECONNECT_ACK` / `GAME_STATE_SYNC` 消息类型
- 房主维护 `disconnectedPlayers` Map 保存掉线玩家状态
- 玩家重连时发送 `RECONNECT_REQUEST`，房主验证后同步游戏状态

**心跳机制：**
- 房主每 5 秒发送心跳给所有客户端
- 客户端每 5 秒回复心跳
- 15 秒无心跳视为掉线

**使用场景：**
- 玩家网络波动暂时掉线
- 玩家切换网络（WiFi → 4G）
- 浏览器意外关闭后重新打开

---

### 3. 玩家踢出功能 - 房主管理权限 ✅

**功能说明：**
- 房主可以踢出任意玩家
- 支持在房间大厅和游戏进行中踢人
- 被踢玩家收到通知并断开连接

**实现细节：**
- 新增 `KICK_PLAYER` / `KICKED` 消息类型
- 房主 UI 添加踢人按钮（红色 ✕）
- 踢人后清理玩家数据并广播通知

**UI 位置：**
- **房间大厅**：玩家列表每个非房主玩家旁边有踢人按钮
- **游戏进行中**：对手视角卡片右上角有踢人按钮

**使用场景：**
- 玩家挂机不参与游戏
- 玩家恶意行为（作弊、骚扰）
- 清理不活跃玩家

---

## 📝 技术改动

### 消息协议扩展（`src/p2p/messages.ts`）

新增消息类型：
```typescript
| 'KICK_PLAYER'        // 房主踢出玩家
| 'KICKED'             // 被踢出通知
| 'HOST_TRANSFER'      // 房主转移
| 'RECONNECT_REQUEST'  // 重连请求
| 'RECONNECT_ACK'      // 重连确认
| 'GAME_STATE_SYNC'    // 游戏状态同步
```

### P2P 连接管理器（`src/p2p/peerConnection.ts`）

**新增属性：**
```typescript
private disconnectedPlayers: Map<...>  // 保存掉线玩家状态
private playerLastHeartbeat: Map<...>  // 心跳时间记录
```

**新增方法：**
- `kickPlayer(targetPlayerId, reason)` - 踢出玩家
- `transferHost()` - 转移房主
- `handleReconnectRequest()` - 处理重连请求
- `startHostHeartbeat()` - 启动心跳检测（房主）
- `startClientHeartbeat()` - 启动心跳（客户端）

**修改方法：**
- `setupHostConnection()` - 添加断线重连逻辑
- `handleHostMessage()` - 处理心跳和重连请求
- `handleClientMessage()` - 处理踢人、房主转移、重连消息

### UI 组件更新

**Lobby.tsx（房间大厅）：**
- 新增 `handleKickPlayer()` 方法
- 玩家列表添加踢人按钮（仅房主可见）

**GameTable.tsx（游戏桌）：**
- `OpponentView` 组件添加踢人按钮
- 支持在游戏进行中踢人

---

## 🧪 测试建议

### 测试场景 1：断线重连
1. 创建房间，加入 2-3 个玩家
2. 开始游戏
3. 一个玩家关闭浏览器
4. 30 秒内重新打开浏览器，输入相同房间号
5. 验证：玩家成功重连，手牌和游戏状态恢复

### 测试场景 2：房主掉线
1. 创建房间，加入 2-3 个玩家
2. 开始游戏
3. 房主关闭浏览器
4. 验证：剩余玩家收到房主转移通知，新房主可以踢人/开始游戏

### 测试场景 3：踢人功能
1. 房主创建房间
2. 加入 2-3 个玩家
3. 房主点击某个玩家旁边的 ✕ 按钮
4. 验证：被踢玩家收到通知并断开，其他玩家看到玩家列表更新

### 测试场景 4：心跳超时
1. 创建房间，加入玩家
2. 一个玩家暂停浏览器（或断网）
3. 等待 15 秒
4. 验证：该玩家被视为掉线，进入重连倒计时

---

## ⚠️ 注意事项

1. **房主转移后游戏状态**：目前新房主继承游戏，但如果新房主也掉线，需要再次转移
2. **重连超时时间**：当前设置为 30 秒，可根据需要调整
3. **心跳间隔**：当前 5 秒发送一次，15 秒超时，可调整
4. **浏览器兼容性**：`alert()` 在移动端可能体验不佳，后续可优化为自定义 UI

---

## 🚀 下一步优化建议

1. **房主转移完善**：实现完整的房主切换逻辑（包括游戏引擎转移）
2. **自定义重连 UI**：替换 `alert()`，使用更好的提示组件
3. **重连倒计时显示**：在 UI 上显示重连剩余时间
4. **玩家重连确认**：重连时询问是否返回原游戏
5. **掉线原因分析**：区分主动离开和网络掉线

---

## 📊 功能完成度

| 功能 | 状态 | 完成度 |
|------|------|--------|
| 房主掉线处理 | ✅ | 80%（需完善房主切换） |
| 断线重连 | ✅ | 90% |
| 玩家踢出 | ✅ | 100% |
| 心跳机制 | ✅ | 100% |

---

**更新时间：** 2026-03-26  
**版本：** v1.2.0  
**作者：** Easyhoov
